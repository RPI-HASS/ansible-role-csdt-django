ADMINS = (
    # ('Your Name', 'your_email@example.com'),
)
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': '{{ csdt_django_secret_db }}',
        'USER': '{{ csdt_django_secret_user }}',
        'PASSWORD': '{{ csdt_django_secret_pass }}',
        'HOST':'127.0.0.1',
        'PORT': '5432',
    }
}
# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '{{ csdt_django_secretkey }}'
# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = {{ csdt_django_settings_debug }}
TEMPLATE_DEBUG = False
PIPELINE_ENABLED = True
WARNING_MESSAGE = None

COMPRESS_ENABLED = True
COMPRESS_OFFLINE = False

ALLOWED_HOSTS = [
{% for item in csdt_django_settings_allowed_hosts %}
    '{{ item }}'{% if not loop.last %},
{% endif %}
{% endfor %}
]

CSRF_TRUSTED_ORIGINS = [
{% for item in csdt_django_settings_csrf_trusted_origins %}
    '{{ item }}'{% if not loop.last %},
{% endif %}
{% endfor %}
]
GOOGLE_ANALYTICS_PROPERTY_ID = '{{ csdt_django_settings_google_analytics_id }}'
GOOGLE_ANALYTICS_DOMAIN = '{{ csdt_django_settings_google_analytics_domain }}'
RECAPTCHA_PRIVATE_KEY = '{{ csdt_django_recaptcha_private }}'
RECAPTCHA_PUBLIC_KEY = '{{ csdt_django_recaptcha_public }}'
EMAIL_API_KEY = '{{ csdt_django_email_API_key }}'
EMAIL_HOST_PASSWORD = '{{ csdt_django_email_API_password }}'
GOOGLE_API_KEY = '{{ csdt_django_Google_API_key }}'
CENSUS_API_KEY = '{{ csdt_django_Census_API_key }}'
